# 調剤薬局専用シフト・勤怠・給与管理システム 新仕様書 (spec.md)

## 1. システム再構築の目的と背景

従来の継ぎ接ぎ開発によって生じていた以下の根本課題をゼロベースで解消し、調剤薬局の現場（三宅管理薬剤師およびスタッフの皆様）が毎日の業務でストレスなく、安全・確実に使えるシステムを新規再構築する。

---

## 2. これまで追加した機能と、判明した不具合・課題の全容

### 2.1 これまで追加した機能（資産）
1. **認証・アカウント管理**:
   - 管理者（薬局長）とスタッフの権限分離
   - スタッフの新規登録・氏名変更・退職（非表示）
2. **シフト管理＆一括編成**:
   - スタッフごとの基本勤務曜日・標準時間・スタッフカラー設定
   - 1ヶ月分の一括自動生成（希望休スキップ、有休自動変換）
   - 月間カレンダー表示（スタッフ別色分け）
   - A4横向き印刷レイアウト（掲示用）
3. **スタッフ機能**:
   - ワンタップ打刻（出勤・退勤・休憩）
   - スマホからの休み希望・有休申請
   - 個人給与シミュレーション・有休残日数確認
4. **クラウド公開＆PWA**:
   - Render上でのWeb公開
   - スマホのホーム画面追加（PWA対応）

---

### 2.2 現在判明している不具合・根本的欠陥（ゼロにする理由）
| No | 不具合・課題 | 原因・技術的背景 | 再構築での根本対策 |
| :--- | :--- | :--- | :--- |
| **1** | **再起動・再デプロイでデータが消える（リセットされる）** | Render無料枠ではSQLiteファイルが再起動ごとに初期化されるため | **クラウド永続データベース（PostgreSQL）を標準採用**し、サーバーが再起動してもデータが100%消えない設計にする。 |
| **2** | **一括シフト作成の不具合・偏り** | 管理者（薬局長）が対象外だった点、および「必要人数」「人員バランス」の考慮不足 | **薬局長含む全スタッフを対象にし、曜日ごとの必要人数（薬剤師・事務）に合わせた確実な自動編成ロジック**を構築。 |
| **3** | **名前変更や設定が画面に反映されない** | 画面リロード不足、およびService Workerの過剰なキャッシュ保持 | **キャッシュバスターの徹底と、更新後の完全自動画面同期**。 |
| **4** | **PC/スマホでの操作フリーズ** | ブラウザのパスワード保存ポップアップと日本語変換確定待ちの干渉 | **ログインUIの導線を簡素化**し、誤認や入力干渉を起こさない設計にする。 |
| **5** | **UIの肥大化・複雑化** | 後からボタンやモーダルを付け足したため、どこを押せば何ができるか直感的でない | **「①シフトを作る」「②出勤を確認する」「③スタッフを設定する」の3大導線に画面をすっきり再整理**。 |

---

## 3. 新システムの基本要件

### 3.1 技術スタック
- **バックエンド**: Python 3.11+ / FastAPI
- **データベース**: PostgreSQL (Render無料Postgres対応) + SQLAlchemy 2.0
- **フロントエンド**: HTML5, Tailwind CSS, Vanilla JS, Lucide Icons
- **モバイル対応**: PWA (キャッシュ事故を起こさないNetwork-First設計)
- **印刷対応**: A4横向きCSSプリントレイアウト

### 3.2 アカウント・権限設計
1. **管理者（管理薬剤師：三宅 智之 様）**:
   - スタッフの登録・編集・削除
   - シフトの一括自動作成・個別調整・A4印刷
   - 休み希望のワンタップ審査
   - リアルタイム勤怠確認、給与集計CSV出力
2. **スタッフ（薬剤師・調剤事務・パート）**:
   - スマホからワンタップ打刻
   - 休み希望（有休・希望休）の簡単提出
   - 自身のシフト確認・給与概算確認

---

## 4. 画面・機能構成（新デザイン）

### 4.1 管理者ポータル (`/admin`)
1. **メイン：月間シフトボード**
   - 誰がいつ出勤するかが一目でわかるカラーカレンダー
   - 「✨ 一括シフト生成」ボタン（人数バランスを崩さないスマートロジック）
   - 「🖨️ A4印刷」ボタン（掲示用シフトが一発印刷）
2. **休み希望審査パネル**
   - スタッフから届いた休み希望を「承認」「却下」するだけ
3. **スタッフ管理パネル**
   - スタッフの追加・名前変更・勤務条件・退職処理を1箇所に集約

### 4.2 スタッフマイページ (`/`)
1. **出勤・退勤打刻カード**（迷わない巨大ボタン）
2. **今月のシフト確認 ＆ 休み希望申請ボタン**
3. **給与・有休残日数メーター**

---

## 5. データモデル設計 (PostgreSQL)

- `users`: ID, ユーザー名, 暗号化パスワード, 氏名, 役職/ロール, 時給, 基本勤務曜日, 標準時間, スタッフ色
- `shifts`: ID, ユーザーID, 日付, 開始時刻, 終了時刻, 休憩, 区分 (通常/有休/公休), 備考
- `shift_requests`: ID, ユーザーID, 対象日, 申請区分 (希望休/有休), 理由, ステータス (申請中/承認/却下)
- `time_records`: ID, ユーザーID, 日付, 出勤打刻, 退勤打刻, 休憩打刻, 実働時間, 修正フラグ
