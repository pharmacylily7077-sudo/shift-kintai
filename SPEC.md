# 調剤薬局専用シフト・勤怠・給与管理システム 仕様書 (SPEC.md)

## 1. システム目的と背景
調剤薬局の現場（三宅管理薬剤師および勤務スタッフの皆様）が、毎日のシフト編成・勤怠打刻・有休管理・給与概算計算を迷わず安全・確実に運用できることを目的としたWeb＆PWAアプリケーション。

---

## 2. 権限・アカウント設計

### 2.1 管理者（管理薬剤師：三宅 智之 様）
- **初期ID / パスワード**: `admin` / `admin123`（管理画面からいつでも変更可能）
- **権限**:
  - 全スタッフの登録・編集・削除（退職処理）
  - 全データ初期化（オールリセット）：三宅様以外の全スタッフ・シフト・打刻を一括消去
  - 自身および各スタッフのパスワード再設定（1クリック生成機能付き）
  - 1ヶ月分シフト一括自動編成（人手不足・薬剤師不在アラート判定付き）
  - 月間カレンダー編成・A4横向き掲示用印刷
  - LINE・メール連絡用確定シフトテキスト一括生成＆コピー
  - 労働基準法「年5日有休取得義務」進捗監視・アラート判定
  - 全テーブル一括ZIPバックアップ（BOM付きExcel対応CSV）
  - 勤怠集計および月次給与集計・CSVダウンロード

### 2.2 スタッフ（薬剤師・調剤事務・パート）
- **初期登録**: 管理者が管理画面から追加（ID、初期パスワード、勤務曜日、標準時間、時給/月給、表示色を設定）
- **権限**:
  - スマホから迷わず押せる巨大2大ボタン（「出勤する」「退勤する」）によるワンタップ打刻
  - 休憩入り・戻りの安全記録
  - 月間シフトの確認および希望休・有休のワンタップ申請
  - 自身の概算給与・有休残日数のリアルタイム確認

---

## 3. スタッフ削除・完全連動仕様

1. **個別スタッフ削除（退職）**:
   - 管理画面「③ スタッフを設定する」のスタッフカード右下のゴミ箱アイコン、または編集モーダル内「このスタッフを削除」から実行。
   - **完全連動処理**:
     - 該当スタッフの全登録シフト（過去・未来）を即時抹消
     - 未処理および過去のシフト希望申請・打刻修正申請を全消去
     - 過去の法定打刻実績がない場合はデータベースから完全物理削除
     - ログイン画面（`/login`）のクイック入力ボタンから即時消去
     - サーバー再起動（Renderデプロイ等）が発生しても、`seed.py` によって勝手に復活しない恒久保護を適用
2. **全データ初期化（オールリセット）**:
   - 管理画面「③ スタッフを設定する」上部の「全データ初期化(リセット)」ボタンから実行。
   - 二重確認ダイアログ（警告・再確認）を経て、**三宅管理薬剤師（Admin）のみを残し、テスト登録した全スタッフ・全シフト・全打刻・全申請を完全一括クリア**。
3. **管理者削除ガード**:
   - 管理者（三宅様）はシステム運用に必須のアカウントであるため、個別削除不可の保護ガードを適用。

---

## 4. パスワード管理仕様

1. **スタッフ新規登録時**:
   - フォーム内で初期パスワード（デフォルト: `staff123` 等）を自由に指定可能。
2. **スタッフ個別パスワード変更（ワンクリック）**:
   - 各スタッフカードの「🔑 PW変更」ボタンから、モーダルを開いて直接更新可能。
   - 「🎲 自動生成」ボタンにより、入力ミスの起きにくい英数字8桁をワンタップ自動発行。
3. **条件編集モーダル連動**:
   - スタッフの時給や出勤曜日を修正する際、必要に応じてパスワードも同時に上書き更新可能（空欄時は変更なし）。
4. **管理者パスワード変更**:
   - 管理画面最上部ヘッダーの「PW変更」ボタンより、三宅様ご自身のログインパスワードをいつでも更新可能。

---

## 5. シフト自動編成＆コンプライアンス仕様

1. **スマート一括編成**:
   - 曜日ごとの固定勤務ルールと標準勤務時間に基づき、月間シフトを一括自動生成。
   - 承認済みの希望休（OFF）は自動スキップ、有休希望は有給休暇シフトに自動変換。
2. **人員バランス判定 (`/api/admin/shifts/balance`)**:
   - 薬剤師不在日やワンオペ・極端な偏りを自動検知し、カレンダー上部に視覚的バナー表示。
3. **年5日有休義務コンプライアンス (`/api/admin/compliance/paid-leave`)**:
   - 労基法第39条第7項（年10日以上付与者に対する年5日取得義務）に完全準拠。
   - 取得ペース未達者を検知し、要対応バナーで警告。
4. **LINE・メール共有用テキスト (`/api/admin/shifts/share-text`)**:
   - 確定した月間シフトを全スタッフ分、および個人宛メッセージ用フォーマットで即時テキスト化・クリップボードコピー。
5. **全データZIPバックアップ (`/api/admin/backup/export`)**:
   - 全データをUTF-8 BOM付きCSV（Excelで開いても文字化けしない）にエクスポートし、ZIP形式で一括保存。

---

## 6. 技術スタック＆アーキテクチャ要件

- **バックエンド**: Python 3.9+ (本番: Python 3.11+) / FastAPI
- **データベース**: PostgreSQL (Render本番) / SQLite (ローカルテスト) / SQLAlchemy 2.0
- **認証方式**: JWT (JSON Web Token) / HttpOnly Cookie + Authorization Bearer
- **フロントエンド**: HTML5, Tailwind CSS, Vanilla JS, Lucide Icons
- **キャッシュ対策**: URLパラメータによるキャッシュバスター (`?v=...`)
