# プロジェクト引き継ぎ書 (handoff.md)

## 1. プロジェクト概要 & 現在の稼働状況
薬局向けシフト勤怠・給与計算・有休管理Webアプリです。
本番環境（Render）へのデプロイおよび永続インフラ化（①）が完了し、安定稼働しています。

- **本番URL**: `https://shift-kintai.onrender.com/admin`
- **初期アカウント**:
  - 管理者: `admin` / `admin123`（表示名: 三宅 興之（薬局長））
  - スタッフ1: `staff01` / `staff123`（小林 彩乃（薬剤師））
  - スタッフ2: `staff02` / `staff123`（寺内（調剤事務））
- **自動テスト**: `tests/test_app.py` 全69テスト通過（実行: `PYTHONPATH=. ./venv/bin/pytest`）
- **技術スタック**: FastAPI + SQLAlchemy + SQLite (WAL) / PostgreSQL + Tailwind CSS / Vanilla JS

---

## 2. 実装完了済みの主要機能
1. **データ永続化＆Render本番デプロイ対応 (完了)**:
   - PostgreSQL接続URL正規化、psycopg2-binary対応、SQLite WALモード設定
2. **スタッフ＆雇用条件管理 (完了)**:
   - スタッフ新規追加、退職処理（非アクティブ化）、氏名変更
   - 曜日ごとの勤務時間・休憩設定、スタッフ色分け設定
   - 給与形態（時給制・固定月給制）の切り替え設定
3. **シフト管理・一括自動生成 (完了)**:
   - 薬局長（管理者）を含む一括シフト自動生成
   - 休み希望（希望休・有休）の自動反映とスキップ
   - A4横向き印刷レイアウト（`window.print()`）
4. **勤怠打刻・実績修正・月次給与集計 (完了)**:
   - スタッフマイページからのワンタップ打刻
   - 管理者による代理打刻・手動実績修正
   - 月次給与集計（実働時間、有休手当、深夜割増、CSVエクスポート）

---

## 3. 次に実装するタスク（仕様書 spec.md / todo.md 準拠）

ユーザー様はプログラミング初心者のため、**選択肢を迫らず、最も壊れにくく堅牢で使いやすい標準仕様**で実装を進めること。

### 【優先タスク1】フェーズ2：現場目線のスマート一括シフト編成エンジン（人員バランス判定）
- 曜日ごとの必要人数（薬剤師・事務）に対する**人員不足・偏りの視覚的警告（アラート）**機能
- 休み希望が重複して特定曜日の人手が不足する場合に、カレンダー上部またはセル内で分かりやすく警告バッジ表示

### 【優先タスク2】フェーズ3：シンプルで迷わないUI/UX整理
- 管理者画面を**「① シフトを作る」「② 出勤を確認する」「③ スタッフを設定する」**の3大導線にすっきり整理
- 画面リロードなしでの即時同期、分かりやすいボタン配置

---

## 4. 新しいチャット用 貼り付けプロンプト

```markdown
handoff.md を読み込んで、次のタスク（「フェーズ2：一括シフト編成の人員バランス判定・偏り警告機能」および「フェーズ3：3大導線UI整理」）の実装を進めてください。
選択肢は求めず、最も使いやすく壊れにくい標準仕様で実装し、最後に全自動テスト（PYTHONPATH=. ./venv/bin/pytest）が全件パスすることを確認してください。
```


