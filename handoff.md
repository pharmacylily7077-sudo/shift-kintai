# プロジェクト引き継ぎ書 (handoff.md)

## 1. プロジェクト概要 & 現在の稼働状況
調剤薬局（ひまわり調剤薬局 / 薬局長: 三宅 興之 様）向けシフト勤怠・給与計算・有休管理Webアプリです。
Render本番環境（PostgreSQL対応）とローカル開発環境（SQLite WALモード）の両方で安定稼働しています。

- **本番URL**: `https://shift-kintai.onrender.com/admin`
- **初期アカウント**:
  - 管理者: `admin` / `admin123`（表示名: 三宅 興之（薬局長））
  - スタッフ1: `staff01` / `staff123`（小林 彩乃（薬剤師））
  - スタッフ2: `staff02` / `staff123`（寺内（調剤事務））
- **自動テスト**: `tests/test_app.py` **全77テスト通過（全件100%パス）**（実行: `PYTHONPATH=. ./venv/bin/pytest`）
- **技術スタック**: FastAPI + SQLAlchemy + SQLite (WAL) / PostgreSQL (psycopg2-binary) + Tailwind CSS + Vanilla JS

---

## 2. 外部本番環境の検証結果（合否判定）

外部HTTPクライアントを用いた実環境（`https://shift-kintai.onrender.com`）検証を実施し、全項目合格を確認済み：

1. **外部疎通テスト**:  **合格**（HTTP 200 OK、Cloudflare経由の暗号化通信正常）
2. **管理者認証・セッションテスト**:  **合格**（三宅 興之 様のアカウントでログイン成功、Cookie発行確認）
3. **データ永続化テスト**:  **合格**（本番DBに初期登録3名が正常保持、データ消失ゼロ確認）
4. **新機能（フェーズ2・3・4）反映状況**:  **合格・デプロイ完了**
   - 人員配置バランス警告（`/api/admin/shifts/balance`）稼働中
   - 3大導線整理（①シフトを作る ②出勤を確認する ③スタッフを設定する）反映済み
   - スタッフ退職・削除機能（ゴミ箱アイコン）反映済み

---

## 3. 調剤薬局現場に向けた「堅牢化＆現場支援」機能の実装完了（フェーズ4）

1. **【便利機能】ワンタップ「シフト確定・LINE一斉送信／メール共有」用テキスト生成**
   - 管理者画面に「💬 LINE共有」ボタンを新設。
   - 全員分一括テキストおよび個別スタッフ別テキストをワンタップで整形し、クリップボードにコピー可能（`/api/admin/shifts/share-text`）。
2. **【堅牢化】管理者画面からの「全データ一括バックアップ（ZIP/CSV）」**
   - 管理者画面上部に「全保存(ZIP)」ボタンを新設。
   - スタッフ・シフト・打刻履歴・希望休・修正申請の全テーブルをExcel文字化け防止BOM付きCSVとしてZIP一括ダウンロード（`/api/admin/backup/export`）。
3. **【簡素化】スタッフスマホ画面の「超直感化（2大ボタン）」**
   - スタッフ画面（スマホ表示）で「出勤する」「退勤する」を特大2大ボタンとして配置。
   - 休憩（入・戻）はサブ操作バーとして分離し、押し間違いを防止。
4. **【法改正・薬事対応】有給休暇 年5日取得義務の進捗アラート**
   - 労働基準法第39条第7項に基づく「年5日有休取得義務」の自動コンプライアンス判定（`/api/admin/compliance/paid-leave`）。
   - 管理者ポータル上部に「要対応スタッフ アラートバナー」を表示し、スタッフカードにも進捗バッジ（達成／進行中／要取得）を表示。

---

## 4. 運用・管理のまとめ

- **本番アクセス**: `https://shift-kintai.onrender.com/admin`
- **データ保護**: クラウドPostgreSQLによる完全永続化 ＋ ワンクリックZIPバックアップ
- **テストコマンド**: `PYTHONPATH=. ./venv/bin/pytest tests/test_app.py`
